# Non-Plugin Files

This folder keeps non-runtime repository files out of the plugin root so the plugin code is quicker to navigate.

- `root-docs/`: release notes, implementation notes, summaries, and other markdown docs not required by runtime
- `tools/`: helper scripts, local test artifacts, and generated files not required by runtime
